import 'package:flutter/material.dart';

class PrisustvaPage extends StatelessWidget {
  const PrisustvaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Prisustva'),
    );
  }
}

