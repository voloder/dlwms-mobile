import 'package:flutter/material.dart';

class DokumentiPage extends StatelessWidget {
  const DokumentiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dokumenti'),
    );
  }
}

