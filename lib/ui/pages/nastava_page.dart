import 'package:flutter/material.dart';

class NastavaPage extends StatelessWidget {
  const NastavaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nastava'),
    );
  }
}

